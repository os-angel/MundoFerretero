name = "mundoferretero"
compatibility_date = "2026-08-08"

# This is an assets-only Cloudflare Worker. Astro generates the static site in
# `dist`; no runtime adapter or Worker entry point is required.
[assets]
directory = "./dist"
